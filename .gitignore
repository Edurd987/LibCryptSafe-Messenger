build/
*.exe
*.a
*.o
*_log.txt
.gradle/
/android/app/build/
android/.idea/
android/app/.cxx/
android/app/build/
android/build/
android/local.properties
android/openssl.zip
*.zip
relay/node_modules/
# Большие файлы моделей — не коммитим
*.gguf
*.safetensors
relay/^C

# Локальные бэкапы при редизайне
*_BACKUP.kt
*_BACKUP.xml
*_BACKUP
*.bak_*
*.bak
# Temporary debug scripts (session patch tools, not for repo)
add_polldiag.py
fast_jitter.py
finalize_brick3.py
fix_isactive.py
patch_polling.py

# build/test artifacts (Aug 30)
*.exe
*.jar
*.log
*.bak
*.bak-before-*
*.tmp
hello.kt
patch_pin.py

# session backups + one-shot patch scripts (Sep — cover dash-suffix + all patch_*)
*.bak-*
patch_*.py
