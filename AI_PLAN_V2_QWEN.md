# AI-модель: новая стратегия V2 (после провала Gemma 2)

## ПОЧЕМУ СМЕНА
Gemma 2 2B давала стабильный сбой обеими сессиями:
- многоязычная каша после "reason" (razones razoes ragioni...)
- legacy токенизатор unsloth
- одинаковый ответ ALLOW на любой вход (переобучение на шаблон)
- та же каша в родном transformers И в GGUF = проблема в модели/обучении

## НОВЫЙ ПОДХОД
1. БАЗА: Qwen 2.5 1.5B Instruct (современный токенизатор, крепкий русский)
2. ИНСТРУМЕНТ: чистый transformers + PEFT (БЕЗ unsloth)
3. ДАННЫЕ: 1200 security + ~400 обычных русских диалогов
   (чтобы не забыла язык, не зацикливалась после JSON)
4. ФОРМАТ: Qwen chat template <|im_start|>
5. ПРОВЕРКА: тест в transformers СРАЗУ, GGUF только после успеха

## ВАЖНО
- LoRA НЕВОЗМОЖНА без базовой модели (нужна основа Qwen/Llama)
- "обучить с нуля без сторонних" = тупик (нужны млрд токенов, кластер)
- наш датасет security_dataset.jsonl (1200) ЦЕЛ
- адаптер Gemma (старый, битый) есть на ПК как архив

## АКТИВЫ
- security_dataset.jsonl — 1200 примеров (на Colab + можно регенерить)
- populate_db.py — генератор (можно нарастить до 3000+)
- Q8 GGUF старый (Gemma, битый) — в LM Studio, как память

## АРХИТЕКТУРА AI В МЕССЕНДЖЕРЕ: ОДИН МОСТ, РЕЖИМЫ ЧЕРЕЗ ПРОМПТЫ

### Принцип
НЕ три модели и НЕ три моста. ОДНА базовая модель + переключение
поведения через системный промпт. База (Qwen) уже умеет болтать,
играть, рассуждать — дообучать (LoRA) надо ТОЛЬКО узкую роль безопасника.

### Технически
Один JNI-мост к llama.cpp (как crypto-мост), один файл модели:
  - analyze(text)   -> системный промпт безопасника -> JSON-вердикт (LoRA)
  - chat(text)      -> системный промпт собеседника  -> свободный ответ (база)
  - playChess(move) -> системный промпт шахматиста    -> ход (база)

Переключение режима = смена СИСТЕМНОГО ПРОМПТА в коде, НЕ загрузка
другой модели. Так работают все ассистенты (ChatGPT, Claude).

### Когда нужен отдельный LoRA на роль
Только если роль требует узкого формата которого нет в базе:
  - Безопасник -> нужен LoRA (узкий JSON, база не знает)
  - Собеседник -> НЕ нужен (база умеет болтать)
  - Игры       -> скорее НЕ нужен (база знает нотацию)
llama.cpp умеет горячо переключать LoRA-адаптеры к одной базе —
но это оптимизация на потом, не стартовая необходимость.

### КРИТИЧНО для мобильного
Три модели = 3 x 2.5GB RAM = телефон умрёт (OOM).
Подход "одна модель + промпты" — единственно реалистичный для Android.
